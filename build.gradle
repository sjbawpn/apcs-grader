apply plugin: 'java'
apply plugin: 'checkstyle'

repositories {
	mavenCentral()
}

test {
	ignoreFailures = true
}

checkstyle {
	ignoreFailures = true
}

dependencies {
	testCompile group: 'junit', name: 'junit', version: '4.+'
	testCompile files("libs/testHelp.jar")
}
